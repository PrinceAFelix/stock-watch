{
  "name": "discord-nfc-webhook",
  "version": "1.0.0",
  "description": "A Vercel-ready project that handles NFC tag scans and sends messages to Discord via webhook",
  "main": "index.js",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "dev:api": "node dev-server.js",
    "build": "cd frontend && npm install && npm run build",
    "deploy": "vercel --prod",
    "vercel-build": "npm run build"
  },
  "keywords": [
    "nfc",
    "discord",
    "webhook",
    "vercel",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.11.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "vercel": "^32.7.2"
  }
}
